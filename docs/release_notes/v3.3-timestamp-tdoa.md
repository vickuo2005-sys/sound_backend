# V3.3 Timestamp TDOA

Adds backend localization after Event Fusion.

Added:

- `localization_results`
- `GET /localization-results`
- `GET /event-groups/{group_id}/localization`
- `POST /event-groups/{group_id}/localize`
- timestamp TDOA solver
- physical consistency and geometry quality checks

Not changed:

- Flutter detection
- MP3 upload
- WAV clip upload
- Event Fusion grouping
